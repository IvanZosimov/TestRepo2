name: Context testing
on: 
    workflow_dispatch:

jobs:
   board-automation:
    strategy:
        matrix:
            os: [windows-latest]
    runs-on: ${{matrix.os}}
    steps:
        - uses: actions/setup-java@v3.6.0
          with:
              java-version: '17'
              distribution: 'temurin'

