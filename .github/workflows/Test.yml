
name: Context testing
on: 
    workflow_dispatch:
    push:
    
jobs:
  build:
    runs-on: ${{matrix.os}}
    strategy:
      matrix:
        os: ['windows-latest']
    steps:
        - name: Checkout tools repo
          uses: actions/checkout@v3
          with:
            repository: actions/setup-java
        - run: |
            ls -lf
