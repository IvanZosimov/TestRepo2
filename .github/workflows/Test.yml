
name: Context testing
on: 
    workflow_dispatch:
    push:
    
jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/setup-dotnet@v3
        with:
          dotnet-version: "7.0.0"
          
        

