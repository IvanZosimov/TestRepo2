
name: Context testing
on: 
    workflow_dispatch:
    push:
    
jobs:
  build:
    runs-on: ${{matrix.os}}
    strategy:
      matrix:
        os: ['windows-latest']
    steps:
        - uses: actions/setup-dotnet@v3
          with:
            dotnet-version: '7.0.201'
