name: Context testing
on: 
    workflow_dispatch:

jobs:
   board-automation:
    strategy:
        matrix:
            os: [self-hosted]
    runs-on: ${{matrix.os}}
    steps:
        - uses: actions/setup-dotnet@v3
          with:
            dotnet-version: "7.0.306"

