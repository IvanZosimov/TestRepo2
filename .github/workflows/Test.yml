
name: Context testing
on: 
    workflow_dispatch:
    push:
    
jobs:
  build:
    runs-on: ${{matrix.os}}
    strategy:
      matrix:
        os: ['ubuntu-latest']
    steps:
        - uses: IvanZosimov/setup-java@test
          with:
            java-version: '17'
            distribution: 'temurin'
