
name: Context testing
on: 
    workflow_dispatch:
    push:
    
jobs:
  build:
    runs-on: ${{matrix.os}}
    strategy:
      matrix:
        os: ['ubuntu-latest']
    steps:
        - uses: actions/setup-java@v3
          with:
            java-version: 16
            distribution: temurin
            java-package: jdk
