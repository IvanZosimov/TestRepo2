
name: Context testing
on: 
    workflow_dispatch:
    push:
    
jobs:
  test1:
    strategy:
        matrix:
          os: ["ubuntu-latest", "macos-latest", "windwos-latest"]
          version: ["6.0"]
    runs-on: ${{matrix.os}}
    
    steps:
      - uses: nogic1008/setup-dotnet@feat/nuget-cache
        with:
          dotnet-version: ${{matrix.version}}
          cache: true
      - run: |
            dotnet build
      - uses: nogic1008/setup-dotnet@feat/nuget-cache
        with:
          dotnet-version: ${{matrix.version}}
          cache: true   
          
        

