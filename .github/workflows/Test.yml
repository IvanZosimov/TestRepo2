name: Context testing
on: 
    workflow_dispatch:

jobs:
   board-automation:
    strategy:
        matrix:
            os: [ubuntu-latest]
            version: ["22-ea"]
    runs-on: ${{matrix.os}}
    steps:
        - uses: actions/setup-java@v3
          with:
            java-version: ${{matrix.version}}
            distribution: "zulu"
        - run: java -version

