name: Context testing
on: 
    workflow_dispatch:

jobs:
   board-automation:
    strategy:
        matrix:
            os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{matrix.os}}
    steps:
        - uses: IvanZosimov/setup-java@dragonwell-test
          with:
            java-version: "17.x"
            distribution: "dragonwell"

