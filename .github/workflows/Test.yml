name: Context testing
on: 
    workflow_dispatch:

jobs:
   board-automation:
    strategy:
        matrix:
            os: [ubuntu-latest]
    runs-on: ${{matrix.os}}
    steps:
        - uses: Accelerator1996/setup-java@main
          with:
            java-version: "8.x"
            distribution: "dragonwell"

