name: Context testing
on: 
    workflow_dispatch:

jobs:
   board-automation:
    strategy:
        matrix:
            os: [ubuntu-latest]
    runs-on: ${{matrix.os}}
    steps:
        - uses: IvanZosimov/setup-java@dragonwell
          with:
            java-version: "11.0.*"
            distribution: "dragonwell"

