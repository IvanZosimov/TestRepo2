
name: Context testing
on: 
    workflow_dispatch:
    push:
    
jobs:
  build:
    runs-on: ${{matrix.os}}
    strategy:
      matrix:
        os: ['ubuntu-latest']
    steps:
        - run: ulimit -a
        - run: ulimit -Ha
        - run: ulimit -Sa
        - run: cat /etc/security/limits.conf
        
