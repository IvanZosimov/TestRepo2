
name: Context testing
on: 
    workflow_dispatch:
    push:
    
jobs:
  test:
    name: Run test
    strategy:
        matrix:
            distrib: ["corretto", "zulu"]
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: IvanZosimov/setup-java@corretto-toolcache-fix
      with:
        java-version: "17"
        distribution: ${{matrix.distrib}}
    - uses: IvanZosimov/setup-java@corretto-toolcache-fix
      with:
        java-version: "17"
        distribution: ${{matrix.distrib}}

