
name: Context testing
on: 
    workflow_dispatch:
    push:
    
jobs:
  build:
    runs-on: ${{matrix.os}}
    continue-on-error: true
    strategy:
      matrix:
        os: ['windows-latest']
    name: setup-node@v3 node-version-file check
    steps:
              
     - uses: IvanZosimov/setup-dotnet@dotnet-install-dir-fix
       with:
          dotnet-version: |
              4.1
     - run: dotnet --info
        
    
        
  
      

