name: 'Test triggering'
on:
  issues:
    types: opened
  pull_request:
    types: opened

jobs:
  stale:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo ${{github.event.sender.login}}
