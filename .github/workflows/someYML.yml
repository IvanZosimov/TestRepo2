
name: Context testing
on: 
    workflow_dispatch:
    push:
    

jobs:
    
  build:
    strategy:
        matrix:
            version: ["3.10"]
    runs-on: macos-latest
    env:
        AGENT_TOOLSDIRECTORY: "/Users/runner/hostedtoolcache/new"
    steps:

    - uses: IvanZosimov/setup-python@main
      with: 
         python-version: "3.10"
         check-latest: true
    - run: |
          echo "$RUNNER_TOOL_CACHE"
          echo "$AGENT_TOOLSDIRECTORY"
          otool -L "${RUNNER_TOOL_CACHE}/Python/3.10.5/x64/bin/python"

    

       
            

  
        
