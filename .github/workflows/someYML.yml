name: Context testing
on: 
    workflow_dispatch:
    push:

jobs:
    first-job:
      runs-on: windows-latest
      steps:
        - uses: actions/checkout@v3
        - uses: IvanZosimov/setup-go@v2
          with: 
            go-version: ^1.17
            check-latest: true
