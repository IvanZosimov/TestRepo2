name: Context testing
on: 
    workflow_dispatch:

jobs:
  first_job:
    strategy:
      matrix:
        node: ['18']
    runs-on: linux-latest
    steps:
      - name: remove npm from the path
        run: | 
            groups
            pwd
            ls -ld
            mkdir po
            cd ..
            pwd
            ls -ld
            mkdir po
            cd ..
            pwd
            ls -ld
            mkdir po
            cd ..
            pwd
            ls -ld
            mkdir po
            cd ..
            pwd
            ls -ld
            cd ..
            pwd
            ls -ld

  
        
