name: Context testing
on: 
    workflow_dispatch:
    push:
    

jobs:
    
  build:
    strategy:
        matrix:
            version: ["3.10"]
    runs-on: self
    - run: |
          echo "${PATH}"
          ldd "${RUNNER_TOOL_CACHE}/Python/3.10.5/x64/bin/python"
          export PATH="$PATH:${RUNNER_TOOL_CACHE}/Python/3.10.5/x64/bin/python"
          echo "${PATH}"
          ldd "${RUNNER_TOOL_CACHE}/Python/3.10.5/x64/bin/python"
       
            

  
        
