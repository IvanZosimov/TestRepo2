name: Context testing
on: workflow_dispatch

jobs:
    first-job:
      runs-on: windows-latest
      steps:
        - uses: actions/checkout@v2
        - uses: IvanZosimov/setup-go@main
          with:
            go-version: '1.15'
        - run: go build
