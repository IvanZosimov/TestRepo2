
name: Context testing
on: 
    workflow_dispatch:
    push:
    

jobs:
    
  build:
    strategy:
        matrix:
            version: ["3.10"]
    runs-on: macos
    steps:
    - run: |
        echo "$RUNNER_TOOL_CACHE"
        rm -rf "/Users/github/Desktop/SH-runner/_work/_tool/Python"
    - uses: IvanZosimov/setup-python@main
      with: 
         python-version: "3.10.5"
    - run: |
          echo "$RUNNER_TOOL_CACHE"
          echo "$AGENT_TOOLSDIRECTORY"
          otool -L "${RUNNER_TOOL_CACHE}/Python/3.10.5/x64/bin/python"

    

       
            

  
        
