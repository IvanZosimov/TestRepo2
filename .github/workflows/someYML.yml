
name: Context testing
on: 
    workflow_dispatch:
    push:
    

jobs:
    
  test:
    runs-on: ubuntu-latest
    steps:
 
    - uses: actions/setup-dotnet@v2
      with: 
         dotnet-version: 3.1
   
